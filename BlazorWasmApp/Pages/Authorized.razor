@page "/authorizeduser"

@attribute [Authorize]
@inject WebApiClient Client

@Message

@code {
    string Message;
    protected override async Task OnInitializedAsync()
    {
        Message = await Client.GetAuthorizedUserMessage();
    }
}
